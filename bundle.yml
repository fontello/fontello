bundles:
  fontello:
    - fontello

  lib:
    - lib

packages:
  fontello:
    depends:
      - lib

    entries:
      client/fontello

  lib:
    entries:
      client/lib

    vendor:
      - knockout
      - kernel: ./lib/system/client/kernel.js
      - fs: ./lib/system/dummy.js
