bundles:
  fontello:
    - fontello

  lib:
    - lib

packages:
  fontello:
    depends:
      - lib

    entries:
      client/fontello

  lib:
    entries:
      client/lib

    vendor:
      # TODO: add lodash after svg2ttf upgrade
      - jquery
      - event-wire
      - babelfish
      - knockout
      - fs: ./lib/system/dummy.js
