---
bundles:
  frontend:
    - fontello

packages:
  fontello:
    client:
      root: ./client/
      include: "**/*.js"
      exclude: /(^|\/)_.*/

    views:
      root: ./client/
      include: "**/*.jade"
      exclude: /(^|\/)_.*/
