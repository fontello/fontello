fontomas:
  max_glyphs: 100
  glyph_size:
    min: 12   # minimal size
    val: 16   # default size
    max: 30   # miximum size
locales:
  enabled: [en]
  default: en
listen:
  host: localhost
  port: 3000
