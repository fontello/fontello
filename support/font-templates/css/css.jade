- if (_.any(glyphs, function (g) { return 0xffff < g.code; }))
  | @charset "UTF-8";
  != '\n'
  != '\n'

:stylus_nowrap
  @font-face {
    font-family: '../font/#{font.fontname}';
    src: url('../font/#{font.fontname}.eot');
    src: url('../font/#{font.fontname}.eot?#iefix') format('embedded-opentype'),
         url('../font/#{font.fontname}.woff') format('woff'),
         url('../font/#{font.fontname}.ttf') format('truetype'),
         url('../font/#{font.fontname}.svg##{font.fontname}') format('svg');
    font-weight: normal;
    font-style: normal;
  }
 
  [class^="icon-"], [class*=" icon-"]
    font-family '#{font.fontname}'
    font-style normal
    speak none

    display inline-block
    width 1.1em
    margin-right .1em
    text-align center


- var i, g, css, hex, chr
- for (i in glyphs)
  - g = glyphs[i]
  - css = meta.css_prefix + g.css
  - hex = '\\' + g.code.toString(16)
  - chr = unichr(g.code)
  != '\n'
  - if (0xffff < g.code)
    | .#{css}:before { content: '#{chr}'; } /* '#{hex}' */
  - else
    | .#{css}:before { content: '#{hex}'; } /* '#{chr}' */
