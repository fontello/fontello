/*global window*/


// vim:syntax=javascript


(function () {
  'use strict';


  var nodeca = window.nodeca = {
    config: {
      fontomas: <%- JSON.stringify(config('fontomas')) %>
    },
    runtime: {
      env:      "<%= env() %>",
      version:  "<%= version() %>"
    }
  };


  nodeca.logger = {
    assert : $.noop,
    error  : $.noop,
    info   : $.noop,
    debug  : $.noop
  };
}());
